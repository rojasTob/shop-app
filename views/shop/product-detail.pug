extends ../layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/form-control.css")
block content
    main
        div.centered.gray-box
                h1.text-centered #{title}
                img.centered(src=image, alt="")
                h3.text-centered $ #{price}
                h3.text-centered(rows="5" enable="false") #{description}
                include ../includes/add-cart
